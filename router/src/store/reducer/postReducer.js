const postBaseState = {
    
}

const postReducer = (state = postBaseState,action) => {
    switch(action.type) {

        default:
            return state
            
    }
}

export default postReducer